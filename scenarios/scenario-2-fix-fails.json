{
  "name": "scenario-2-fix-fails",
  "description": "Same alert as scenario-1, but with a hand-crafted bad patch (timeout set to 5000ms — too long to trip the actual 1.5s tail). Verifier should REJECT. This is the scenario that proves the verifier is willing to disagree with the AI-SRE.",
  "repo_path": "sample-app",
  "use_claude": false,
  "alert": {
    "event_id": "dd-evt-9c1f8b",
    "alert_title": "[p99 high] checkout latency above SLO",
    "title": "[p99 high] checkout latency above SLO",
    "event_msg": "Monitor 'checkout.p99.latency' triggered. p99 over the last 10 minutes is 1,420ms; SLO is 300ms.",
    "body": "Monitor 'checkout.p99.latency' triggered. p99 over the last 10 minutes is 1,420ms; SLO is 300ms.",
    "service": "checkout",
    "metric": "checkout.http.request.duration.p99",
    "alert_threshold": 300,
    "last_value": 1420,
    "alert_transition": "error",
    "tags": {
      "service": "checkout",
      "env": "staging",
      "team": "payments"
    }
  },
  "prepared_patch": {
    "summary": "Add 5000ms timeout to fetch_price (too generous — won't trip the actual tail).",
    "hypothesis": "The pricing call is occasionally slow. Adding a timeout will bound the worst case.",
    "repro_steps": [
      "Drive 100 checkout requests",
      "Observe p99 latency"
    ],
    "files": [
      {
        "path": "app/checkout.py",
        "before": "\"\"\"Checkout endpoint.\n\nPLANTED BUG: `fetch_price` is called with no timeout. When the pricing service\nhas tail latency (which it does — that is the live cluster state), checkout p99\nexplodes. The AI-SRE should propose wrapping the call in a timeout. The\nverifier then proves the patch actually fixes p99 by running both versions\nunder mirrord against the same pricing service.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom .pricing import fetch_price\n\n\ndef checkout(item_id: str, qty: int) -> dict:\n    # BUG: no timeout on the downstream pricing call.\n    price = fetch_price(item_id)\n    return {\"item_id\": item_id, \"qty\": qty, \"total\": price * qty}",
        "after": "\"\"\"Checkout endpoint (with prepared bad fix: timeout too generous).\"\"\"\n\nfrom __future__ import annotations\n\nfrom .pricing import fetch_price\n\n\ndef checkout(item_id: str, qty: int) -> dict:\n    price = fetch_price(item_id, timeout_ms=5000)\n    return {\"item_id\": item_id, \"qty\": qty, \"total\": price * qty}"
      }
    ],
    "confidence": 0.7,
    "expected_signal_change": "p99 drops below 300ms",
    "model": "scenario-prepared"
  }
}
